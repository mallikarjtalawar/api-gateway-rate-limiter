# api-gateway-rate-limiter
A distributed API gateway with a Redis-backed sliding-window rate limiter — proven correct under concurrency and across multiple gateway instances, with circuit breaking, async usage logging, and chaos-tested failure handling.
